# Integration Tests

Use integration tests for behavior across modules, APIs, persistence boundaries,
or frontend route interactions.

Example file name:

```text
tc_integration_score_event_to_box_score.md
```

