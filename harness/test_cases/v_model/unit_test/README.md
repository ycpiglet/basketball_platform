# Unit Tests

Use unit tests for deterministic business logic, validation helpers, and pure
state transitions.

Executable backend unit tests should live in `backend/tests/`.
Executable frontend unit tests should live in `frontend/src/tests/`.

