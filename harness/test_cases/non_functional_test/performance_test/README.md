# Performance Tests

Use performance tests for response time, throughput, scoreboard sync latency,
and large-screen display responsiveness.

Use `load_test/` for expected traffic and `stress_test/` for beyond-normal
conditions.

